### Docker Command

```bash
docker ps --format "table {{.ID}}\t{{.RunningFor}}\t{{.Names}}\t{{.Mounts}}"
```

### Close Salt

```bash
sudo systemctl stop putwall_gui.service
```

### Radial Command

```bash
/var/covariant/app/data/radial-wms-ftp-mock/admin

sudo rm /var/covariant/app/state/practice_batch/* -rf

ALLOW_UNCOMMITTED_CHANGES=True ./putwall.sh u_shape/radial/szx_02 api down -m practice_batch && ./putwall.sh u_shape/radial/szx_02 gui down

ALLOW_UNCOMMITTED_CHANGES=True ./putwall.sh u_shape/radial/szx_02 api up -m practice_batch

./putwall.sh u_shape/radial/szx_02 gui up

```

### Geodis Command

```bash

./cov_apps/application/putwall/u_shape/geodis/mock_geodis_server/generate_wave_file.py \
  --min-parcel-size 2 --max-parcel-size 5 \
  --short-picks-per-parcel 1 --number-of-short-pick-parcels 2 \
  ./202304030055001/

# Then, copy tote config files to
rm -rf /var/covariant/app/state/geodis/mock_geodis_server/tote_config_files/*
cp *.json /var/covariant/app/state/geodis/mock_geodis_server/tote_config_files/
# copy wave files to
rm -rf /var/covariant/app/data/geodis-wms-ftp-mock/admin/*
cp WV* /var/covariant/app/data/geodis-wms-ftp-mock/admin/

sudo rm /var/covariant/app/state/practice_batch/* -rf

ALLOW_UNCOMMITTED_CHANGES=True ./putwall.sh u_shape/geodis/szx_01 api down -m practice_batch && ./putwall.sh u_shape/geodis/szx_01 gui down

ALLOW_UNCOMMITTED_CHANGES=True ./putwall.sh u_shape/geodis/szx_01 api up -m practice_batch

./putwall.sh u_shape/geodis/szx_01 gui up

```
